# 💾 MinIO 对象存储指南

## 1. 这是什么？(What is this?)
MinIO 是一个**高性能的对象存储系统**，完全兼容 **Amazon S3 API**。
*   **核心特点**: 把文件存成"对象"，就像云端的硬盘，但支持超大规模（EB 级）。
*   **应用场景**: AI/ML 数据集存储、视频/图片 CDN、大数据分析、备份归档。
*   **物理位置**: `modules/minio`

## 2. 为什么需要它？
*   **自托管 S3**: 不想花钱用 AWS S3？MinIO 让你在自己服务器上搭建一个"私有 S3"。
*   **高性能**: 专为 AI/ML 数据流水线优化，读写速度极快。
*   **跨平台**: 支持 Docker、Kubernetes、裸机部署。

## 3. 小白安装教程

### 重要提示
**MinIO 社区版现已改为源码发布**（不再提供预编译二进制）。
如果需要企业级支持和预编译版本，请前往 [AIStor](https://min.io/download)。

### 方式 A：从源码安装 (推荐)
需要先安装 **Go 1.24+**。[安装 Go 指南](https://golang.org/doc/install)

```bash
# 一键编译安装
go install github.com/minio/minio@latest
```

### 方式 B：Docker 部署 (最简单)
```bash
# 拉取官方镜像
docker pull minio/minio

# 启动服务 (把 /data 挂载到本地)
docker run -p 9000:9000 -p 9001:9001 \
  -v ~/minio-data:/data \
  minio/minio server /data --console-address :9001
```

*   浏览器打开 `http://localhost:9001`
*   默认账号：`minioadmin`
*   默认密码：`minioadmin`

## 4. 快速开始 (启动后)
1.  **创建存储桶 (Bucket)**:
    *   在 Web 界面点击 "Create Bucket"，输入名字（如 `my-data`）。
2.  **上传文件**:
    *   点击进入桶，点 "Upload" 上传图片/视频。
3.  **获取访问链接**:
    *   右键文件 -> "Share"，复制链接给前端用。

## 5. 配合代码使用
MinIO 完全兼容 AWS S3 SDK。例如用 Python 上传文件：

```python
from minio import Minio

client = Minio(
    "localhost:9000",
    access_key="minioadmin",
    secret_key="minioadmin",
    secure=False  # 本地测试用 HTTP
)

# 上传文件
client.fput_object("my-data", "photo.jpg", "/path/to/photo.jpg")
```

## 6. 常见问题
*   **Q: 和 AWS S3 有什么区别？**
    *   A: MinIO 完全兼容 S3 API，但部署在你自己的服务器上，不收流量费。
*   **Q: 适合做网站图床吗？**
    *   A: 非常合适！配合 Nginx 反代或 CDN 使用。

## 7. 资源地址
*   **GitHub**: [minio/minio](https://github.com/minio/minio)
*   **官方文档**: [docs.min.io](https://docs.min.io)
