# 🚀 Strapi 官方 CMS 指南

## 1. 这是什么？(What is this?)
Strapi 是目前全球最火的 **Headless CMS** (无头内容管理系统)。
*   **传统 CMS (WordPress)**: 给你一套固定的模板 + 后台，你在这里写文章，前台直接展示。
*   **Headless CMS (Strapi)**: 只给你一个**超级强大的后台 API**。你可以用各种你喜欢的技术（React, Vue, 小程序, iOS）去取数据展示。
*   **物理位置**: `modules/strapi`

## 2. 为什么你需要它？
如果你正在做一个 App 或网站，需要一个后台来**管理文章、产品、用户数据**，但不想花太多时间手写后端代码（Node.js/Python）。
Strapi 能帮你**一键生成 API**。
*   你只需要在界面上点点点：“我要一个 Product 表，有字段 Name, Price”。
*   它自动给你生成 `/api/products` 接口。
*   还送你一个漂亮的后台管理界面。

## 3. 小白安装教程 (3分钟上手)
Strapi 是基于 Node.js 的，所以你需要先安装 Node.js。

### 第一步：一键创建
打开终端，进入你想放项目的目录：

```bash
# 使用 npx 创建新项目 (推荐)
npx create-strapi@latest my-project --quickstart
```
*(注意：`my-project` 是文件夹名，你可以随便改，比如 `my-blog`)*

### 第二步：启动
如果上面的命令成功跑完，它通常会自动启动并打开浏览器。如果没有：

```bash
cd my-project
npm run develop
```

### 第三步：注册管理员
1.  浏览器会自动打开 `http://localhost:1337/admin`。
2.  第一次需要注册一个超级管理员账号。
3.  注册完就进入后台了。

## 4. 核心功能速览
*   **Content-Type Builder (建模)**: 最强大的功能。在这里创建“文章表”、“商品表”。
*   **Content Manager (录入)**: 给运营人员用的，像 Word 一样录入数据。
*   **Settings (API 权限)**: 默认生成的接口是不公开的。去 *Settings -> USERS & PERMISSIONS PLUGIN -> Roles -> Public*，把 `find` 和 `findOne` 勾上，外界才能访问接口。

## 5. 常见问题
*   **Q: 安装很慢？**
    *   A: 可能是 npm 源的问题。尝试切换国内镜像源或使用 `yarn`。
*   **Q: 怎么部署到线上？**
    *   A: 推荐使用 **Docker** 部署，或者使用 Strapi Cloud (官方托管)。
*   **Q: 它和 WordPress 比哪个好？**
    *   A: 做纯展示型网站（博客/企业站）且不想写代码，用 WordPress。做 App 后台、小程序后台或定制化很高的网站，用 Strapi。

## 6. 资源地址
*   **GitHub**: [strapi/strapi](https://github.com/strapi/strapi)
*   **官方文档**: [docs.strapi.io](https://docs.strapi.io)
