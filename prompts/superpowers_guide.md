# ⚡️ Superpowers 生产力脚本集指南

## 1. 这是什么？
这是一堆**黑客的瑞士军刀**。
作者 obra 写这套脚本的逻辑是：“如果一件事我要在这个终端做两次，我就必须把它写成脚本。”
这里面包含了自动化 Git 操作、文件处理、搜索等几十个极速小工具。

## 2. 最强安利：这几个脚本超好用

### 🎯 `git-wtf` (Git What The F*ck)
*   **痛点**: 打 `git status` 看不懂分支乱七八糟的状态？
*   **功能**: 它会用极其人话的方式告诉你：你的分支和主分支差了多少？有没有冲突？谁合并了谁？
*   **用法**: 终端输入 `git-wtf` 回车。

### 🧹 `git-delete-merged`
*   **痛点**: 项目做久了，本地几百个废弃的分支，看着心烦。
*   **功能**: 一键删除所有**已经合并进主分支**的本地分支。安全又爽快。
*   **用法**: 终端输入 `git-delete-merged` 回车。

## 3. 小白安装教程 (Mac/Linux)
要用这些命令，你必须把它们“告诉”你的系统。

### 第一步：确认路径
假设你现在的 King-Toolbox 项目在桌面。
终端输入： `pwd`
记下显示的路径，比如 `/Users/King/Desktop/King-Toolbox`。

### 第二步：一键配置 (Zsh用户)
复制下面这行代码（**注意替换你的真实路径**），在终端执行：

```bash
# 注意：把 /Path/To 换成你上面的真实路径
echo 'export PATH="/Path/To/King-Toolbox/modules/superpowers/bin:$PATH"' >> ~/.zshrc
```

**示例（假设在桌面）**:
```bash
echo 'export PATH="$HOME/Desktop/King-Toolbox/modules/superpowers/bin:$PATH"' >> ~/.zshrc
```

### 第三步：生效
执行：
```bash
source ~/.zshrc
```

### 第四步：验证
输入 `git-wtf`。如果输出了内容而不是报错 `command not found`，恭喜你，你已经拥有超能力了！

## 4. 资源地址
*   **GitHub**: [obra/superpowers](https://github.com/obra/superpowers)