# 🧩 Awesome Claude Skills 使用指南

## 1. 这是什么？(What is this?)
这是一个**AI 技能扩展包**。
想象一下，Claude 本来是一个通用的“大脑”，但它不懂怎么**写深度长文**、**做 SEO 调研**或者**管理 Gmail**。
这个库就是给 Claude 准备的“技能书” (Skills)。一旦你让它“阅读”或“挂载”了这些技能，它就瞬间变成了某个领域的垂直专家。

## 2. 核心价值
*   **省事**: 不用自己写几千字的 Prompt，直接用现成的最佳实践。
*   **专业**: 每个技能都包含明确的角色设定 (Role)、行动指南 (Workflow) 和质量标准。

## 3. 快速上手教程 (你是哪种玩家？)

### 玩法 A：复制粘贴流 (适合 Trae/ChatGPT/Web 用户) - **最简单**
如果你平时主要在网页版或 Trae 对话框里用 AI，选这个。

1.  **找到技能**:
    *   在左侧文件树打开 `modules/awesome-claude-skills` 文件夹。
2.  **选择职业**:
    *   比如你想写一篇高质量文章，找到 `content-research-writer` 文件夹。
    *   打开里面的 `SKILL.md` 文件。
3.  **激活**:
    *   全选复制 `SKILL.md` 的内容。
    *   粘贴给 Trae/Claude，并附上你的任务：
    > "请扮演这个角色，帮我写一篇关于《2026年 AI 发展趋势》的文章。"

### 玩法 B：极客命令行流 (适合 Claude CLI 用户)
如果你使用 Anthropic 官方的终端工具 `claude` (Anthropic's CLI)，可以将技能“安装”进去。

1.  **准备环境**:
    *   确保你安装了 Claude CLI (`npm install -g @anthropic-ai/claude-code`)。
2.  **建立连接**:
    *   在终端运行以下命令 (将当前目录的技能连到 Claude 配置里)：
    ```bash
    # 创建技能存放目录
    mkdir -p ~/.claude/skills

    # 举例：安装“内容研究员”技能
    ln -s "$(pwd)/modules/awesome-claude-skills/content-research-writer" ~/.claude/skills/content-research-writer
    ```
3.  **召唤**:
    *   在 `claude` 命令行里直接说："Use the content-research-writer skill to analyze this topic..."

## 4. 推荐尝试的技能
*   `content-research-writer`: 深度长文写作（强烈推荐）。
*   `code-reviewer`: 帮你 review 代码，找 bug。
*   `seo-optimizer`: 优化你的文章关键词。

## 5. 常见问题
*   **Q: 文件夹是空的？**
    *   A: 你可能没有下载子模块。请在终端执行：`git submodule update --init --recursive`
